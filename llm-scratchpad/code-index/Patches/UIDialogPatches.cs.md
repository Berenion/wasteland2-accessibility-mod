File: Patches/UIDialogPatches.cs — Formerly contained ModalMessageMenu_SetMessage_Patch; now empty. DialogState.AnnounceDialog() handles all dialog content announcements.

namespace Wasteland2AccessibilityMod.Patches  (line 3)

// No active patch classes. ModalMessageMenu_SetMessage_Patch was removed because it fired before
// DialogState.AnnounceDialog()'s SpeakInterrupt, causing the text to be cut off.
